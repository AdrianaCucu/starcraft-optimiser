package resources;

public class Gas extends Resource {

    /**
     * Stores the gas gathered since the beginning of the game (so static).
     * Starts at 0 and keeps incrementing.
     */
    public static double total = 0;
}
