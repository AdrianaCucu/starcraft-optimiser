package resources;

public class Minerals extends Resource {

    /**
     * Stores the minerals gathered since the beginning of the game (so static).
     * Starts at 0 and keeps incrementing.
     */
    public static double total = 0;
}
